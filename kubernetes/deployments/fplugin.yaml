apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: fplugin
  name: fplugin
  namespace: default
spec:
  selector:
    matchLabels:
      app: fplugin
  template:
    metadata:
      labels:
        app: fplugin
    spec:
      containers:
      - image: gcr.io/fantasy-218701/fplugin:03cc3cdb202e3f8c67aa3c129b64a5567222f5ec
        name: fplugin
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /alive
            port: 8080
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
